export default class RegisterEvent {
    constructor(amountOwnership, totalPrice, eventId, userId) {
      this.amountOwnership = amountOwnership;
      this.totalPrice = totalPrice;
      this.eventId = eventId;
      this.userId = userId;
    }
  }